version: "3"
services:
  bot:
    image: medios_bot
    deploy:
      replicas: 1
    ports:
      - "80:8000"
    networks:
      - red
    depends_on:
      - db
  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: laSextaDeclaracion
    networks:
      - red
networks:
  red:
